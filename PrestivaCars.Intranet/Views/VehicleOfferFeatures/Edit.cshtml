@model PrestivaCars.Data.Data.Vehicles.VehicleOfferFeature

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>VehicleOfferFeature</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="VehicleOfferFeatureId" />
            <div class="form-group">
                <label asp-for="VehicleOfferId" class="control-label"></label>
                <select asp-for="VehicleOfferId" class="form-control" asp-items="ViewBag.VehicleOfferId"></select>
                <span asp-validation-for="VehicleOfferId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="VehicleFeatureId" class="control-label"></label>
                <select asp-for="VehicleFeatureId" class="form-control" asp-items="ViewBag.VehicleFeatureId"></select>
                <span asp-validation-for="VehicleFeatureId" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="IsActive" /> @Html.DisplayNameFor(model => model.IsActive)
                </label>
            </div>
            
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
